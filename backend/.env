DB_HOST=localhost
DB_PORT=5432
DB_NAME=dial_hackathon
DB_USER=postgres
DB_PASSWORD=postgres

# Server
PORT=3000

# Secrets (API keys, number ids, webhook secrets) go in backend/.env.local —
# it is git-ignored (*.local) and overrides this file. See .env.local for the keys.
