# components/agents/

UI for Agents Under Test.

- `AgentList.tsx` — grid/list of agents with platform badges.
- `AgentCard.tsx` — single agent summary (name, platform, phone number).
- `AgentForm.tsx` — create/edit an agent (name, system prompt, platform).
