# components/benchmark/

UI for "Us vs competitors".

- `PlatformColumn.tsx` — one platform's result (status + scorecard).
- `BenchmarkTable.tsx` — side-by-side comparison across Dial / VAPI / ElevenLabs on the
  same criteria.
