# components/scorecard/

UI for judge results.

- `OverallScore.tsx` — headline run-level score.
- `CriterionRow.tsx` — one criterion: pass/fail + score + justification.
- `Scorecard.tsx` — composes the overall score and the list of criterion rows.
